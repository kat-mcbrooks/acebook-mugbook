<%= form_for @post do |form| %>
  <%= form.text_area :message, :placeholder => "Say something!", cols: 40, rows: 4, class: 'text-box' %><br>
  
  <%= form.file_field :image %><br>
  <%= form.hidden_field :user_id, :value => current_user.id %>

  <%= form.submit "Submit" %>
<% end %>